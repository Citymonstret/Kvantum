A generator for HTML markup

HTML html = new HTML();
html.setDoctype(HTML.DOCTYPE_HTML);

Will use the builder pattern
html.setDoctype(HTML.DOCTYPE_HTML)
  .setTitle("An example")
  .header()
  .add(
    new Style().rule(new Rule("#div").set("margin-left", "10px"))
  );
